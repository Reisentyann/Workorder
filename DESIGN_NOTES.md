# AI 工单处理工作台 — 设计笔记

> 本文记录已确定的全部需求、约束与设计决策，供实现时对照，避免遗漏。

## 1. 题目要求（来源：AI Coding 实战题-（pf后端））

实现最小可用的 **AI 工单处理工作台**，帮助客服高效处理工单。重点在「AI 自动分析、结论验证、后续处理」链路，不在完整 CRUD。

### AI 分析结果必含 7 字段（硬性）
1. 工单分类
2. 优先级
3. 摘要
4. 置信度 / 判断把握度
5. 判断依据 / 证据
6. 建议处理角色 / 团队 / 人员
7. 是否适合自动处理或自动修复

> 补充：置信度由 **LLM 自己判断输出**（mock 阶段模拟）；优先级规则做**金额敏感**（退款金额超阈值 → 高优先级）。

### 功能要求
- 创建工单、查看列表、查看详情、AI 分析
- 对 AI 结果做补充验证 / 给判断依据
- 展示 AI 结果、人工确认或修改 AI 建议
- 对适合的问题给自动处理 / 自动修复建议
- 对信息不足 / 结论不明的问题给补充信息 / 人工接管建议
- 更新工单状态

### 交付内容
- 源代码 + 项目说明（启动方式、测试方式、AI 分析/验证/自动处理思路）
- 建议 30~45 分钟完成

### 明确不要求
- 登录鉴权、复杂权限、线上部署、完整派单
- 自动处理/修复的**真实执行**（给建议 / patch 思路 / 模拟即可）

---

## 核心能力（评分重点，已实现）

题目第 17 行明确：重点在「AI 自动分析、结论验证、后续处理」，不在 CRUD。三者均已实现，落点如下：

### A. AI 自动分析（`internal/analyzer`）
- **`LLMClient` 接口**隔离：`MockLLM` 用规则 + 关键词模拟，可换真实 LLM 不改上层。
- **策略模式 + 工厂 + 注册表**：每种工单类型一个 `Strategy`（退款/登录异常/发票/物流/兜底），`Factory.Register` 挂载，新增类型只增不改（开闭原则）。
- **路由**：关键词特征命中 → 对应策略；未命中 → 兜底「其他」。
- 产出 7 字段 + 信息完整性初判；置信度由 LLM 自身判断（人工指令 > 强命中 > 兜底）。

### B. 结论验证（`internal/verifier`，独立于分析）
- **证据一致性**：判断依据为空 → 下调把握度。
- **历史案例交叉验证**：与已确认数据集做 bigram Jaccard 相似度，一致加权、无同类下调。
- **信息完整性**：缺关键字段 → `needs_more_info` + 补充清单。
- **拒答 + 三档分级**：`<0.6` 拒答转人工（附 `refusal_summary` 已尝试摘要）；`0.6~0.9` 拟稿+人工确认；`≥0.9` 自动处理建议。
- 校准后的置信度回写分析结果。

### C. 后续处理能力（自动处理 + 人工兜底）
- **可自动处理**：登录异常等给出 `auto_fix_suggestion`（自助重置/验证码引导步骤）。
- **信息不足/低置信**：给出补充清单或人工接管建议。
- **人工确认/修改**：确认后回写工单，案例**计入数据集**（数据飞轮，反哺验证）。
- **熔断**：连续低质 3 次或累计 5 次分析 → 升级 `escalated`，拒绝再次自动分析。
- **强行打断 + 超时**：Esc/按钮中断，30s 超时兜底。

---

## 2. 技术选型（已确定）

- **语言**：Go（后端）
- **前端**：原生 HTML/CSS/JS 单页应用，放 `/frontend`
- **后端**：Go，放 `/backend`
- **存储**：JSON 文件，**不用数据库**
- **AI**：不接真实 LLM，规则 + 关键词 + mock 置信度即可（真实接入或 mock 均可）

---

## 3. 存储与持久化约束

- **不用数据库表**：避免工单删除时的级联问题、难以处理。
- **用 JSON 文件存储**：工单、分析结果、预设动作、队列等均存 JSON。
- **持久化状态**：每次操作后落盘。
- **懒标记软删除**：更新/删除时**不物理删除重建**，用 `deleted: true` + `deleted_at` 标记。
- **原子写**：先写临时文件再 rename，防止中途崩溃写坏文件。
- **加锁**：多请求并发写 JSON 需串行化（文件锁或进程内锁）。
- **稳定 ID**：用 UUID，不用自增序号（软删后序号会错乱）。
- **中文编码**：UTF-8，写入 JSON 时 `ensure_ascii=False`（Go 里默认 UTF-8，无需转义）。

## 4. 状态与审计

- **操作留痕（审计日志）**：每次操作（创建/分析/确认/状态变更/删除/执行动作/演示）记一条审计，**独立存 `audit.json`**，供追责。埋点统一在 store 层（`addAuditLocked`），任何调用方（api/engine/demo）都会自动记录。
- **分析结果版本化**：重做分析不覆盖旧结果，保留 `analysis_history[]`，每条带时间戳。
- **状态机校验**：状态流转需合法性校验；懒标记只用于删除，不用于状态流转。
- **历史工单浏览**：`GET /tickets?include_deleted=true` 返回含软删工单，配合审计日志实现追责。

### 状态枚举
`pending`(待处理) → `analyzing`(分析中) → `needs_review`(待确认) → `resolved`(已处理) / `needs_info`(需补充信息) / `canceled`(已取消) / `escalated`(已升级人工，熔断)

## 5. 工单来源与处理队列

- **工单来源（服务器推送模拟）**：收件箱（inbox）预置多条用户反馈消息，模拟上游推送；使用者点击「处理」→ 用消息内容创建工单 → 进入 AI 分析。
- 工单进 **FIFO 队列**，每次把**队首**给人工审查。
- 审查入口：`/api/v1/queue/pop`（pop 队首），审查完成放回 / 标记完成。
- **重做插队**：让 LLM 重做的工单，放到处理队列**最前面**（`PushFront`），优先被审查。
- 队列持久化到 JSON。

## 6. 设计模式（需体现）

- **工厂模式**：`HandlerFactory` 根据工单类型返回对应处理器，创建逻辑集中。
- **策略模式 + 注册表 + 开闭原则（OCP）**：定义 `TicketHandler` 接口，每种工单类型一个实现，`Register(type, handler)` 挂载；新增类型只增不改（对扩展开放、对修改封闭）。这就是「增量代码」模式。

## 7. 扩展方式（已确定）

- **不加脚本引擎**（不加 goja/gopher-lua/JSON 规则热加载），保持零额外依赖。
- **开闭原则写在代码里**：策略模式 + 注册表 + 工厂。新增工单类型 = 新增一个 Go 策略文件 + `Register(type, handler)`，不修改已有代码，重新编译一次即生效。
- 为隔离未来替换能力，抽象 `LLMClient` 接口（`MockLLM` 实现），可换真实 LLM 而不改上层。

## 8. 快速操作（预设动作）系统

人工审查者不想反复打字跟 AI 对话，用**预置按钮/快捷键**一键触发。

- 点击按钮 = 对当前工单执行一条**预设指令**（如「重做分析，把分类改成退款」），后端把指令拼进 prompt 重跑。
- 支持**快捷键**触发（1-9）。
- 支持**快速新增按钮**：用户自定义指令 → 落地为新按钮/快捷键，一键复用。
- 预设动作库**持久化到 JSON**，重启不丢。

三层结构：**预置动作库 → 触发（按钮/快捷键）→ 套用当前工单上下文执行并回写**。

## 9. 强行打断 / 取消

AI 卡住时能**主动中断**，不等它跑完。

- 取消入口：按钮 + 快捷键（Esc）。
- 后端任务可取消（可取消的执行体 + 取消标志），打断即终止并释放连接。
- **超时兜底**：即使没手动打断也设超时（如 30s）自动中止。
- **打断回写状态**：取消后工单状态落回（如 `pending`/`canceled`），不残留 `analyzing` 卡死。
- 前端状态复位：打断后按钮/加载态复位，允许重新触发。
- **幂等 / 防重复触发**：快捷键连点不能重复触发分析。

## 10. 接口规范

- 所有接口用**版本前缀**：`/api/v1/xxx`，便于接口版本迭代。

---

## 11. API 契约（前端已按此实现）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/v1/tickets` | 工单列表（`?include_deleted=true` 含软删） |
| POST | `/api/v1/tickets` | 创建工单 |
| GET | `/api/v1/tickets/{id}` | 工单详情 |
| DELETE | `/api/v1/tickets/{id}` | 软删除 |
| PUT | `/api/v1/tickets/{id}/status` | 更新状态 |
| POST | `/api/v1/tickets/{id}/analyze` | 触发分析（可带 `instruction`） |
| POST | `/api/v1/tickets/{id}/analyze/cancel` | 打断分析 |
| PUT | `/api/v1/tickets/{id}/analysis/{aid}/confirm` | 确认/修改 AI 建议 |
| GET | `/api/v1/queue` | 查看队列 |
| GET | `/api/v1/queue/pop` | 取队首工单审查 |
| GET | `/api/v1/actions` | 预设动作列表 |
| POST | `/api/v1/actions` | 新增预设动作 |
| DELETE | `/api/v1/actions/{id}` | 删除预设动作 |
| POST | `/api/v1/tickets/{id}/actions/{aid}` | 执行预设动作 |
| GET | `/api/v1/audit` | 审计日志（`?ticket_id=` 过滤） |
| GET | `/api/v1/inbox` | 推送消息列表（收件箱） |
| POST | `/api/v1/inbox/{id}/handle` | 处理推送消息（创建工单） |
| POST | `/api/v1/mock/preset` | 设置演示场景桩（仅 `-demo`） |
| POST | `/api/v1/mock/reset` | 重置场景桩（仅 `-demo`） |
| POST | `/api/v1/demo/run` | 运行演示场景（仅 `-demo`） |
| GET | `/api/v1/demo/logs` | 演示日志（仅 `-demo`） |

## 12. 目录结构

```
frontend/    index.html, style.css, app.js
backend/     Go 后端（含 handlers/ 脚本目录、JSON 数据目录）
```

## 13. 置信度、金额敏感与数据集（补充决策）

- **置信度由 LLM 自己判断**：`confidence` 直接由 LLM 输出（mock 阶段模拟），不用外部规则算分。
- **金额敏感优先级**：优先级规则含金额抽取，退款金额超阈值（如 500）→ 高优先级，需从正文抽取数字。
- **数据集机制**：LLM 每次分析结果经**人工验证（确认）**后，将该工单计入**数据集**（历史案例库），供验证器做历史案例交叉验证；未经人工验证的工单不入库。

## 14. 日志系统

- 用 Go 标准库 **`log/slog`**（结构化日志，零额外依赖，Go 1.21+）。
- 级别：`debug / info / warn / error`。
- 记录内容：HTTP 请求、AI 分析开始/完成/打断、验证结果、人工确认/修改、状态流转、软删除、预设动作执行、错误与堆栈。
- 输出：控制台 + 可选文件（JSON 格式，便于排查）。
- **与「审计日志」区分**：审计日志是**业务数据**（存 JSON、计入数据集、供验证器交叉验证）；系统日志是**运行排查日志**，两者职责不同、分开存储。

## 15. AI 处理机状态机（执行层）

> 稳定且可控：明确的初始态/终态、可打断、超时兜底、崩溃恢复一致。

### 状态枚举

```
(无) --submit--> QUEUED --start--> RUNNING --success--> SUCCEEDED
                                    |
                                    +--cancel--> CANCELING --ack--> CANCELED
                                    +--timeout--> TIMED_OUT
                                    +--error--> FAILED
```

终态：`SUCCEEDED / CANCELED / TIMED_OUT / FAILED`（不可逆，需显式 resubmit）。

### 状态转换表

| 当前态 | 事件 | 下一态 |
|---|---|---|
| — | submit | QUEUED |
| QUEUED | start | RUNNING |
| RUNNING | success | SUCCEEDED |
| RUNNING | cancel | CANCELING |
| CANCELING | ack | CANCELED |
| RUNNING | timeout | TIMED_OUT |
| RUNNING | error | FAILED |
| 终态 | resubmit | QUEUED |

### 与工单状态联动

- `QUEUED / RUNNING / CANCELING` → 工单 `analyzing`
- `SUCCEEDED` → 工单 `needs_review`（进入人工确认）
- `CANCELED / TIMED_OUT / FAILED` → 工单 `canceled` / `pending`（回落）

### 可控性保障

1. **单 worker 串行**消费 channel，天然无竞态。
2. **cancel 幂等**：非 `RUNNING` 态收到 cancel 直接返回，不产生副作用。
3. **超时兜底**：`context.WithTimeout` + `select`，防永久挂起。
4. **状态持久化**：每个转换写 JSON，崩溃重启读到终态，不残留 `RUNNING`。
5. **终态不可逆**：避免重复消费。

## 16. 可靠性（降级与恢复，链路级保障）

每个处理链路都可能出错，做了如下降级与恢复：

| 链路 | 出错场景 | 降级/恢复 |
|---|---|---|
| LLM 分析 | 出错 | `fail` → 分析标 `failed`，工单回落 `pending` |
| LLM 分析 | 超时 | `timedOut` → 分析标 `timed_out`，工单回落 `pending`（可重试） |
| LLM 分析 | 打断 | `canceled` → 工单 `canceled`；用 `errors.Is` 精确区分，消除打断竞态 |
| 结论验证 | panic | `Verify` 内 recover，降级跳过校准，不拖垮分析 |
| 回写结果 | 存储写失败 | 检查错误，失败则回落 `pending` |
| 存储落盘 | 磁盘写/rename 失败 | `slog.Error` 告警，不静默忽略 |
| 进程崩溃 | analyzing 残留 | **启动恢复**：`Start` 时扫描 `analyzing` 工单，把 `running` 分析标 `failed`，工单回落 `pending`，记审计 |

> 工单数据本身不会丢：每次操作原子写 JSON（临时文件 + rename），崩溃后数据仍在，只是状态被启动恢复兜底。

## 17. 演示与测试（参考验证）

- **引导式演示**（前端）：选场景后系统**逐步引导用户操作**——提示「点哪里、输入什么」，高亮目标元素，用户操作后进入下一步。替代早期"自动跑+看日志"（对使用者无价值）。
- **五个引导场景**：① 完整主流程 ② AI 错误答案+人工修正 ③ 低置信度→拒答 ④ 慢分析+打断 ⑤ 信息不足→补充建议。
- **Mock 场景桩**：`POST /api/v1/mock/preset` 预置速度/置信度/错误答案（引导场景用它模拟异常），`/mock/reset` 恢复。仅 `-demo` 启动时注册。

## 18. 风控与输入治理（拒答 / 熔断 / 脱敏）

- **拒答（Refuse-to-Answer）**：置信度 `<0.6` 或命中「其他」兜底 → `refused=true` + `human_takeover=true`，附 `refusal_summary`（已尝试摘要），不硬答。
- **熔断（Circuit Breaker）**：连续低质 3 次 或 累计分析 5 次 → 工单 `escalated`，审计记录 `escalate`，`analyze` 返回 409 拒绝再次自动分析。
- **输入脱敏（`internal/sanitize`）**：分析前对手机号/身份证/邮箱/银行卡正则脱敏，原始工单仍完整存储供人工查看。

## 19. 待办 / 待确认

- [x] 后端主体实现 + 单元测试（基本测试必须交付）
- [x] 项目说明 README（启动方式、测试方式、AI 分析/验证/自动处理思路）
