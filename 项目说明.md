# AI 工单处理工作台 — 项目说明

> 详细记录项目特点、AI 处理链路、错误处理与降级分析。与 `README.md`（启动说明）、`DESIGN_NOTES.md`（设计决策）互补。

## 一、项目定位

最小可用的 **AI 工单处理工作台**，定位是「**AI 辅助提效 + 人工兜底**」（Human-in-the-loop），不是「AI 全自动客服」。

- AI 只给**分析和处理建议**，不直接执行；
- 最终结果由**人工确认或修改**；
- 自动处理只给**建议/思路**，不真实执行；
- 全程留**审计日志**，可追责。

对应题目评分重点：**AI 自动分析、结论验证、后续处理能力**（三者均已实现）。

## 二、项目特点

| 维度 | 特点 |
|---|---|
| 工单来源 | 服务器推送用户反馈（消息面板模拟），点击反馈直接建工单 |
| 存储 | JSON 文件，无数据库；原子写（临时文件 + rename）；懒标记软删除 |
| AI 分析 | 策略模式 + 工厂 + 注册表，新增类型只增不改（开闭原则） |
| LLM 隔离 | `LLMClient` 接口，`MockLLM` 可换真实 LLM 不改上层 |
| 置信度 | 由 LLM 自己判断；三档分级（≥0.9 自动建议 / 0.6~0.9 拟稿+确认 / <0.6 拒答） |
| 优先级 | 金额敏感（退款金额超阈值 → 高优先级） |
| 结论验证 | 证据一致性 + 历史案例交叉验证 + 信息完整性，独立于分析层 |
| 拒答 | AI 不确定时不硬答，附「已尝试摘要」转人工 |
| 熔断 | 连续低质 3 次或累计 5 次 → 升级人工，拒绝再次自动分析 |
| 输入脱敏 | 分析前对手机号/身份证/邮箱/银行卡脱敏，原始工单完整保留 |
| 自动处理 | 登录异常等给出自助修复建议（操作步骤） |
| 人工闭环 | 确认/修改后回写工单，案例计入数据集反哺验证 |
| 快速操作 | 预设按钮 + 快捷键（1-9），一键触发预设指令；支持动态新增 |
| 强行打断 | Esc / 按钮中断分析，取消幂等；30s 超时兜底 |
| 处理机 | 单 worker 串行，状态机稳定可控 |
| 队列 | FIFO，重做工单插队到队首 |
| 审计 | 独立 `audit.json`，store 层统一埋点，可追责 |
| 历史浏览 | 含软删的全部工单 + 每个工单的操作时间线 |
| 演示 | 引导式演示（逐步提示点哪里、输入什么），5 个场景 |
| 日志 | `log/slog` 结构化，与业务审计分离 |
| 启动恢复 | 进程崩溃后清理残留 analyzing 工单，不卡死 |
| 依赖 | Go 标准库零外部依赖；前端原生 HTML/CSS/JS |

## 三、完整工作流

```
服务器推送用户反馈（消息面板）
        │ 客服点击消息
        ▼
   创建工单（pending）+ 入队 + 审计
        │ 触发 AI 分析
        ▼
   脱敏 → AI 分析（路由→策略→7字段）→ 验证 → 回写
        │ 工单 needs_review
        ▼
   人工查看结果 → 确认/修改
        │
        ├─ 可自动处理 → 自动处理建议
        ├─ 信息不足   → 补充信息清单
        └─ 低置信/拒答 → 人工接管
        ▼
   工单 resolved（案例计入数据集）
```

## 四、AI 处理链路（详细）

### 链路 1：工单创建（`store.HandleMessage`）
- 点击推送消息 → 用消息内容建工单（标题取前 20 字，内容为完整消息）
- 工单状态 `pending`，入 FIFO 队列
- 记审计 `create`

### 链路 2：触发分析（`engine.Submit`）
- 校验工单状态（已熔断 `escalated` → 返回 `ErrEscalated`，API 层映射 409）
- 创建 `running` 分析记录（版本化，不覆盖历史）
- 工单状态 → `analyzing`
- 带指令的重做工单插队到队首（`EnqueueFront`）
- 任务入 channel，记审计 `analyze`

### 链路 3：执行分析（`engine.run` → `llm.Analyze`）
- 设置 `context.WithTimeout`（30s）+ 注册 cancelFunc
- **脱敏**：`sanitize.Mask` 对标题/内容脱敏后送入 LLM
- **路由**：关键词特征命中 → 对应策略；未命中 → 兜底「其他」
- **人工指令注入**：预设动作的 instruction 强制路由到指定策略
- **7 字段产出**：分类、优先级（金额敏感）、摘要、置信度（LLM 自判）、判断依据、建议角色、是否可自动处理
- **信息完整性初判**：缺关键字段 → `needs_more_info` + 补充清单 + 下调置信度

### 链路 4：结论验证（`verifier.Verify`，独立于分析）
- **证据一致性**：判断依据为空 → 下调把握度
- **历史案例交叉验证**：与已确认数据集做 bigram Jaccard 相似度，一致加权、无同类下调
- **拒答判定 + 三档分级**：
  - `<0.6` 或命中「其他」→ `refused=true` + `human_takeover=true` + `refusal_summary`（已尝试摘要）
  - `0.6~0.9` → 拟稿 + 人工确认
  - `≥0.9` → 自动处理建议
- 校准后的置信度回写分析结果

### 链路 5：回写结果（`engine.succeed`）
- `UpdateAnalysis`（status=done）—— 失败则回落 `pending`（`fail`）
- 工单状态 → `needs_review`
- 记审计 `analyze`（分析完成）
- **熔断检查**（`checkCircuitBreaker`）：连续低质 3 次或累计 5 次 → `escalated` + 审计 `escalate`

### 链路 6：人工确认/修改（`store.ConfirmAnalysis`）
- 回写工单（分类/优先级/摘要/建议角色）
- 工单状态 → `resolved`
- **计入数据集**（反哺验证器的历史案例库）
- 记审计 `confirm`

### 链路 7：状态流转 / 队列
- 人工可手动更新状态（合法状态校验）
- `/queue/pop` 取队首工单审查

## 五、错误处理与降级分析

> 假设每个链路都可能出问题，逐链路给出出错场景与降级措施。

### 5.1 链路 × 降级矩阵

| 链路 | 出错场景 | 降级措施 | 落点 |
|---|---|---|---|
| 工单创建 | 存储写失败 | 返回错误给前端，工单不创建 | `store.CreateTicket`/`HandleMessage` |
| 触发分析 | 工单不存在 | 返回 `ErrNotFound` → 404 | `engine.Submit` |
| 触发分析 | 工单已熔断 | 返回 `ErrEscalated` → 409，拒绝自动分析 | `engine.Submit` + `api.analyze` |
| LLM 分析 | **出错** | `fail`：分析标 `failed`，工单回落 `pending` | `engine.fail` |
| LLM 分析 | **超时** | `timedOut`：分析标 `timed_out`，工单回落 `pending`（可重试） | `engine.timedOut` |
| LLM 分析 | **打断** | `canceled`：分析标 `canceled`，工单 `canceled` | `engine.canceled` |
| LLM 分析 | 打断竞态 | `errors.Is(ctx.Err(), ...)` 精确区分超时/取消，消除 `select` 随机选 case 的不确定 | `engine.run` |
| 结论验证 | **panic** | `Verify` 内 `recover`，降级跳过校准，用原始结果，不拖垮分析 | `verifier.Verify` |
| 回写结果 | 存储写失败 | 检查错误，失败则回落 `pending` | `engine.succeed` |
| 存储落盘 | 磁盘写/rename 失败 | `slog.Error` 告警，不静默忽略 | `store.save`/`saveAudit` |
| 进程崩溃 | analyzing 残留 | **启动恢复**：`Start` 时扫描 analyzing 工单，running 分析标 `failed`，工单回落 `pending`，记审计 | `engine.recoverStale` |
| 熔断 | 连续低质/累计超限 | 工单 `escalated`，拒绝再次自动分析（409） | `engine.checkCircuitBreaker` |
| 信息不足 | 缺关键字段 | `needs_more_info` + 补充清单，不硬给结论 | `analyzer` 信息完整性 |
| 低置信 | 把握度不足 | 拒答 + 人工接管 + 已尝试摘要 | `verifier` 拒答判定 |

### 5.2 关键降级设计说明

**1. 超时与取消精确区分（消除打断竞态）**

之前 `select` 里 `ch`（LLM 结果）和 `ctx.Done()` 同时就绪时 Go 随机选一个，导致打断结果不确定（可能回落 pending 也可能 canceled）。现在用 `errors.Is` 精确分类：

```go
case out := <-ch:
    if out.err != nil {
        switch {
        case errors.Is(out.err, context.Canceled):    e.canceled(j)
        case errors.Is(out.err, context.DeadlineExceeded): e.timedOut(j)
        default: e.fail(j, out.err)
        }
    ...
case <-ctx.Done():
    if errors.Is(ctx.Err(), context.DeadlineExceeded) { e.timedOut(j) } else { e.canceled(j) }
```

**2. 启动恢复（防崩溃卡死）**

工单数据原子写持久化，**不会物理丢失**。但进程崩溃时 `analyzing` 工单和 `running` 分析会残留。`engine.Start` 时 `recoverStale` 扫描清理：

```go
for _, t := range store.ListAllTickets() {
    if t.Status != StatusAnalyzing { continue }
    // running 分析标 failed，工单回落 pending，记审计
}
```

**3. 验证器 panic 兜底**

验证器崩了不能拖垮整个分析。`Verify` 用 `defer recover` 降级：跳过校准，返回原始分析结果 + 一条降级原因。

**4. 存储失败不静默**

`save`/`saveAudit` 失败记 `slog.Error`；`engine.succeed` 回写结果失败时检查错误并回落 `pending`，不让工单卡在错误中间态。

## 六、设计模式与工程亮点

| 模式 | 应用 |
|---|---|
| **策略模式 + 工厂 + 注册表** | 每种工单类型一个 `Strategy`，`Factory.Register` 挂载，新增类型只增不改（开闭原则） |
| **接口隔离** | `LLMClient` 抽象 LLM，`MockLLM` 可换真实实现 |
| **状态机** | AI 处理机 `QUEUED→RUNNING→SUCCEEDED/CANCELED/TIMED_OUT/FAILED`，单 worker 串行 |
| **懒标记** | 软删除用 `deleted` 标记，不物理删除重建 |
| **原子写** | 临时文件 + rename，防中途崩溃写坏 |
| **数据飞轮** | 人工确认的案例计入数据集，反哺验证器交叉验证 |
| **幂等取消** | 非 RUNNING 态收到 cancel 直接返回；pendingCancel 处理任务未启动时的取消 |

## 七、可靠性保障总结

1. **数据不丢**：每次操作原子写 JSON 落盘。
2. **状态不卡**：启动恢复清理 analyzing 残留；超时/失败回落 pending 可重试。
3. **降级完整**：每个链路出错都有降级（fail/timeout/cancel/recover/告警）。
4. **人工兜底**：拒答、熔断、信息不足都转人工，AI 不硬答、不硬执行。
5. **全程可追**：独立审计日志 + 历史工单浏览 + 操作时间线。

## 八、技术栈与目录结构

- 后端：Go 标准库（`net/http` + `log/slog`），零外部依赖
- 前端：原生 HTML/CSS/JS
- 存储：JSON 文件（`data.json` + `audit.json`）

```
backend/
  cmd/workbench/main.go        # 入口
  internal/
    model/      # 数据模型 + 状态枚举 + UUID
    store/      # JSON 存储（原子写/锁/软删除/队列/数据集/审计/收件箱）
    analyzer/   # AI 分析（LLMClient + MockLLM + 策略 + 工厂 + 路由）
    verifier/   # 结论验证（证据/历史案例/拒答/三档分级）
    engine/     # AI 处理机（单 worker 状态机 + 打断 + 超时 + 启动恢复 + 熔断）
    sanitize/   # 输入脱敏
    demo/       # 脚本化演示（备用）
    api/        # /api/v1 接口层
  data/         # 运行时 JSON
frontend/       # 单页应用
start.bat / start.ps1   # 一键启动
```

## 九、启动与测试

```bash
cd backend
go run ./cmd/workbench -demo     # 演示模式（含 mock preset）
# 或双击 start.bat
go test ./...                    # 单元测试
```

前端访问 `http://localhost:8080`，点「▶ 演示」选场景，系统逐步引导操作。
